// Sort an array, 3
var arr = [
  [1, 3, 4],
  [2, 4, 6, 8],
  [3, 6]
];

var newArray = [];
var sumList = [];

function sumArray(arr) {
    var sumList = arr.reduce(function(value, item) {

    });
}

arr.sort(function(a, b) {
  return (sumArray(a) - sumArray(b));
});

